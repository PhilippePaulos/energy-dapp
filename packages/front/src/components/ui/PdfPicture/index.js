import PictureAsPdf from "@mui/icons-material/PictureAsPdf";
import { styled } from "@mui/material";

const PdfPicture = styled(PictureAsPdf)({
    '&': {
        cursor: "pointer"
    }
});

export default PdfPicture